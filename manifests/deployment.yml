apiVersion : apps/v1beta1
kind: Deployment
metadata:
  name: default 
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: default 
    spec:
      containers:
        - name: tt_micro_bl 
          image: faustoarci.azurecr.io/tt_micro_bl 
          ports:
          - containerPort: 6001